# Spotify Web API credentials for `npm run sync:catalog`.
# Create an app at https://developer.spotify.com/dashboard
# (any redirect URI works — Client Credentials flow doesn't use it).
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
